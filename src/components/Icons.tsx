import { Calculator, BookOpen, FlaskConical, History, GraduationCap, ChevronLeft, CheckCircle2, XCircle, Bot, X, Send, MessageCircle } from 'lucide-react';

export const Icons = {
  Calculator,
  BookOpen,
  FlaskConical,
  History,
  GraduationCap,
  ChevronLeft,
  CheckCircle2,
  XCircle,
  Bot,
  X,
  Send,
  MessageCircle
};

export type IconName = keyof typeof Icons;
